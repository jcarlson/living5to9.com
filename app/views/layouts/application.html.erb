<!DOCTYPE html>
<html>

    <head>
      <title>living5to9.com</title>
      <%= stylesheet_link_tag :application %>
      <%= javascript_include_tag :application %>
      <%= csrf_meta_tag %>
    </head>

    <body>

        <div class="zone header">
            <div class="section">
                <h1><a href="index.html">living5to9.com</a></h1>
                <%= render 'layouts/navigation' %>
            </div>
        </div>

        <div class="zone body">
            <div class="section">
                <%= yield %>
            </div>
        </div>

        <div class="zone footer">
            <div class="section">
                <%= render 'layouts/navigation' %>
            </div>
        </div>

    </body>

</html>
